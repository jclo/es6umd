/* eslint */

/**
* This library ...
*
* @namespace lib
*/
const lib = {

/**
 * Returns an array.
 *
 * @method ()
 * @public
 * @param {}          -,
 * @returns {Array}   returns an array,
 * since 0.0.1
 */
  array() {
    return [1, 2, 3];
  },

/**
 * Returns a string.
 *
 * @method ()
 * @public
 * @param {}          -,
 * @returns {String}  returns a string,
 * since 0.0.1
 */
  string() {
    return 'I am a string!';
  },
};

export default lib;
